@using TestASP.Data.Enums;
@using TestASP.Web.Models.ViewModels.Questionnaires;

@model List<SubQuestionAnswerViewModel>
@{            
}

@if(Model?.Count > 0)
{
    @foreach (var subQuestion in Model)
    {
        <partial name="_AnswerQuestion" model="@subQuestion" />
    }
}

